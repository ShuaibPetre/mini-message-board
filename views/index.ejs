<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="./new">Add a comment!</a>
    <div> 
      <% for( let i = 0; i < messages.length; i++ ) { %>
        <h2><%= messages[i].user %> </h2>
        <p><%= messages[i].text %> </p>
        <p><%= messages[i].added %> </p>
      

    <% } %>
        
      </div>
  </body>
</html>
